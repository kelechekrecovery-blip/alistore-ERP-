# Gate 0 truth-baseline acceptance — 2026-08-06

## Status and SHA boundary

**Gate 0 truth-baseline software is accepted; external launch remains blocked.**
Independent code, TypeScript/API-contract and security reviews are approved. The
implementation commit below is the immutable boundary for the Task 6 files and all
reported product behavior.

- Pre-program code baseline: `c4963477395b091726fbd587f3f523d46408e24e`.
- Gate 0 Tasks 1–5 reviewed boundary: `3e40b79e`.
- Current candidate base: `1b6283e48d0438a0d37718fedca122bc5729872e`.
- `fcdcf64c` (`fix(auth): expose production login methods honestly`) advanced HEAD
  concurrently and is outside Task 6.
- `1b6283e4` (`fix(auth): load official Apple sign-in SDK`) advanced HEAD again
  concurrently and is outside Task 6.
- Resulting reviewed Task 6 implementation commit:
  `611348e4476f0c189829a4173888f9fc5cc56785`.
- Unrelated preserved worktree change: `apps/web/tsconfig.json`.

The acceptance amendment containing this evidence is a documentation-only child of the
implementation commit. Post-commit focused tests proved that all Task 6 paths still match
the implementation SHA byte-for-byte. The pre-existing `apps/web/tsconfig.json` worktree
change remained unstaged and is not part of either commit. This evidence is not provider
evidence or production certification and does not claim external launch readiness.

## Reviewed Gate 0 commit chain before Task 6

| Task | Reviewed commits |
|---|---|
| Baseline capture | `a484cad5` |
| Migration/rollback authority | `370324dc`, `2bd0d043` |
| Executable surface matrix | `9f7e8093`, `5386d7b4`, `89bee326` |
| Feature-flag registry | `a66e62f2`, `05fbb2f6` |
| Feature-flag adoption/cutover | `7def5c7b`, `e04d71fb`, `1c6d8d25`, `acf243d2`, `3e40b79e` |

Review status and per-commit evidence are recorded in
`.superpowers/sdd/2026-08-06-gate-0-truth-baseline/task-{1..5}-report.md`.

## Task 6 behavior

The staff-only readiness report and ERP view now use exactly four row states:
`missing`, `configured`, `certified`, and `blocked`. Credentials or an adapter can
produce at most `configured`; `certified` requires a deploy-owned operator attestation
after the manual close criteria in `docs/LAUNCH-BLOCKERS.md`.

New rows are `native_push_ios`, `outbox_health`, `meilisearch`, `native_links`,
`backup_restore`, and `partner_payout_provider`. Attestation variables are ordinary
deployment controls, not mutable feature flags. They are mutable environment assertions,
not code-validated evidence or SHA bindings, and must be reset after relevant provider,
model, callback, domain, policy, device or release changes. Output exposes variable names
only and never values. The four-state contract is v2; the original route remains a pure v1
projection for rolling compatibility. Demo mode is explicit and strict CLI acceptance always
fails closed in demo mode.

## TDD evidence

| Command | Exact result |
|---|---|
| `npm test -w @alistore/api -- --runInBand test/external-readiness.spec.ts` without `TEST_DATABASE_URL` | Exit 1 at global setup: required isolated test database missing. Precondition failure; not counted as behavioral RED. |
| `TEST_DATABASE_URL='postgresql://alistore@localhost:5432/alistore_test?schema=public' npm test -w @alistore/api -- --runInBand test/external-readiness.spec.ts` before implementation | Expected RED: exit 1; 1 suite failed; 3 failed, 11 passed. Missing rows, configured-vs-certified behavior and certification markers failed. |
| Same focused API command after implementation | GREEN: exit 0; 1 suite, 14/14 tests passed, 0 failed; 12.204 s. |
| `npm test -w @alistore/web -- components/erp/ReadinessView.test.tsx` initial component harness | RED: exit 1; 0 tests collected because Vitest did not resolve `@/lib/cn` through `Card`. |
| Same Web command after a local `Card` mock and relative DTO import | GREEN: exit 0; 1 file, 1/1 test passed; 498 ms. |

The API tests cover the six rows, missing/configured/certified transitions, explicit
markers, public-demo blocking behavior, and secret-safe serialization. The Web rendering
test proves configured and certified are visually distinct, renders the marker name, and
does not render credential values.

## Final implementation-SHA validation results

| Command | Result |
|---|---|
| `node --test scripts/__tests__/gate0-baseline.test.mjs scripts/__tests__/ecosystem-surface-matrix.test.mjs` | Exit 0; 26/26 passed. |
| `TEST_DATABASE_URL='postgresql://alistore@localhost:5432/alistore_test?schema=public' npm test -w @alistore/api -- --runInBand src/health/health.controller.spec.ts test/external-readiness.spec.ts` | Exit 0; 2 suites and 19/19 tests passed. |
| `npm test -w @alistore/web -- lib/api/readiness.test.ts components/erp/ReadinessView.test.tsx` | Exit 0; 2 files and 20/20 tests passed. |
| `npm run api:contract:matrix` | Exit 0; 421 API contracts written. |
| `npm run ecosystem:matrix:strict` | Exit 0; 51 rows and 51/51 surfaces covered. |
| `E2E_AUTO_PREPARE_DB=false E2E_DATABASE_URL=...alistore_gate0_20260806_task6_test... E2E_API_PORT=4286 E2E_WEB_PORT=3286 npm run web:route-audit` | Exit 0; Chromium 46/46 passed in 11.5 minutes on a newly created isolated local database. The shared `alistore_test` attempt stopped before tests on unrelated duplicate legacy values. |
| `npm run api:build` | Exit 0; Prisma Client generated and API TypeScript production build passed. |
| `npx tsc -p apps/api/tsconfig.json --noEmit` | Exit 0; API typecheck passed with no diagnostics. |
| `npm test -w @alistore/web` | Exit 0; 33 files and 203/203 tests passed. |
| `npm run build -w @alistore/web` | Exit 0; production build compiled/typechecked and generated 44 pages. Expected dynamic-server warning for the no-store storefront-block request was non-fatal. |
| Post-commit focused API readiness tests | Exit 0; 2 suites and 19/19 tests passed against implementation commit `611348e4`. |
| Post-commit focused Web readiness tests | Exit 0; 2 files and 20/20 tests passed against implementation commit `611348e4`. |
| Post-commit Gate 0 baseline/surface tests | Exit 0; 26/26 tests passed against implementation commit `611348e4`. |
| `git diff --check && git diff --cached --check` | Exit 0 before commit and after the implementation commit. All 15 Task 6 paths matched `611348e4` byte-for-byte before this evidence-only amendment. |
| `npm run ecosystem:audit:strict` | Exit 1 before audit execution: trusted-NPM evidence policy rejected ignored Next.js env file `.env.production`. This is an evidence-environment blocker, not a passing audit or a product result; the controller must rerun from an allowed final-SHA environment. |
| `ANDROID_HOME=/Users/alistore/Library/Android/sdk npm run android:build` | Exit 0 with pinned OpenJDK 17; `BUILD SUCCESSFUL`, 170 actionable tasks, and all four debug APKs built. An earlier full incremental packaging attempt failed transiently; targeted package reruns and this final full rerun passed. |
| Isolated pinned iOS scheme build (`xcodebuild ... -scheme AliStoreUITests ... -derivedDataPath /tmp/alistore-gate0-task6-derived build`) | Exit 0 with Xcode 26.6 / Swift 6.3.3; `BUILD SUCCEEDED` across the Client, Staff, Courier, POS, Core and UI-test dependency graph. The package-isolated run supersedes the first shared-DerivedData failure caused by missing package module maps. Simulator/emulator builds do not certify physical APNs/FCM, biometrics, camera, scanner, printer, terminal or native links. |

## Known blockers and non-claims

- `outbox_health` cannot be certified from config. `/api/observability/status` exposes
  failed depth and oldest pending age, but not oldest failed/DLQ age. Certification needs
  a time-bound attached query artifact or a separately reviewed endpoint extension.
- APNs/FCM, Meilisearch, native links, partner payout provider, fiscal, payment/refund,
  SMS, object storage, monitoring and POS hardware remain external/live gates. A true marker
  only records the responsible operator's attestation; this code does not validate the named
  evidence file, environment or SHA.
- The local 2026-07 backup drills prove software procedure only. No production-bucket or
  production Evidence-object restore is claimed here.
- No provider, hardware, physical-device, live Outbox health, production restore, store
  submission, owner signoff, legal acceptance, first-store UAT or pilot claim was executed
  or inferred by Task 6.

## Final acceptance checklist

- [x] Independent code review approved after two SDD fix rounds.
- [x] Independent TypeScript/API contract review approved.
- [x] Independent security review approved for secret safety and readiness honesty.
- [x] Task 6 committed without the unrelated `apps/web/tsconfig.json` change.
- [x] Resulting implementation commit SHA inserted above.
- [x] Required gates rerun on the frozen Task 6 implementation tree and counts updated.
- [x] `git diff --check` and strongest practical trusted ecosystem audit attempt recorded;
  the trusted audit's pre-execution policy blocker is explicitly not reported as a pass.
