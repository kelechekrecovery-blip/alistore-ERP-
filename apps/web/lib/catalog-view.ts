/**
 * Размер страницы каталога. Общий, потому что его обязаны знать оба конца:
 * серверная оболочка (`app/catalog/page.tsx`) запрашивает ровно ту же выборку,
 * которую клиент считает первой страницей, иначе счётчик страниц и кнопка
 * «Дальше» разойдутся с тем, что уже отрисовано.
 */
export const CATALOG_PAGE_SIZE = 24;
